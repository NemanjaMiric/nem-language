# Fibonacci numbers generator

function fib(first, second, stop) (
    while (not 0 and not 0) (
        if(first < stop) (
            temporary = first
            first = second
            second = temporary + second
        ) otherwise break
        continue
    )

    return 0
)

fib(1, 1, 1000)

a1 = [1, null, "\"text\""]
_0 = (- 0.2 + .2) ^ (2 % 2) * (-2 + 02) ^ (2 - 2) + (-2 - 2) ^ (2 + 2) / (2 + 2) ^ (2 + 2) # comments are cool

b = a1[1]

function a() (
    function b()
        return [1, [1, 2, [2, 3]]]

    return b
)

d = convert(a()()[1][2][0], "number")
print([1, 2][0])
print("asd"[1][0])
